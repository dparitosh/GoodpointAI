"""Stray module placeholder.

This file exists with a non-importable name (contains a space) and previously
contained duplicate router code with broken relative imports/indentation.

It is intentionally left as a no-op to avoid static-analysis noise.
"""