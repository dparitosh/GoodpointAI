[pytest]
# Silence pytest-asyncio deprecation by explicitly setting fixture loop scope.
asyncio_default_fixture_loop_scope = function

# Test discovery is scoped to this backend's tests/ directory.
testpaths =
	tests

# Extra safety: never recurse into external directories.
norecursedirs =
	.venv
	node_modules
