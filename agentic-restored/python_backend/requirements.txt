# Consolidated requirements for GraphTrace (FastAPI backend)
#
# This repo previously had multiple requirements files:
#   - requirement.txt
#   - requirements-dev.txt
#   - requirements_external_integrations.txt
#
# These have been consolidated into this single file to simplify installation.
#
# Notes:
# - Some dependencies are optional and only used when you enable/configure a feature.
#   They are included here to make feature enablement frictionless.
# - A few very heavy optional dependencies remain commented out (see bottom) because
#   they are not required for the default FastAPI runtime.

# -----------------------------------------------------------------------------
# Core FastAPI & Web Framework
# -----------------------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
httpx==0.27.2
aiofiles==24.1.0

# -----------------------------------------------------------------------------
# Database & ORM (Postgres-only persistence)
# -----------------------------------------------------------------------------
sqlalchemy==2.0.35
alembic==1.13.1
psycopg[binary]==3.2.3
asyncpg==0.30.0

# Optional DB connectors used by data-source connectivity tests/imports
oracledb>=2.0.0
pyodbc>=5.0.0

# -----------------------------------------------------------------------------
# Graph / Search
# -----------------------------------------------------------------------------
neo4j==5.25.0
opensearch-py==3.1.0

# -----------------------------------------------------------------------------
# NoSQL / Cache (optional)
# -----------------------------------------------------------------------------
pymongo==4.10.1
redis==5.1.1

# -----------------------------------------------------------------------------
# Config / Validation / Serialization
# Note: pydantic pinned <2.11.0 to avoid llama-index/mcp conflicts with 2.10
# Note: pydantic-settings updated for compatibility
# -----------------------------------------------------------------------------
pydantic>=2.7.0,!=2.10.0  # Pinned to avoid llama-index/mcp conflicts with 2.10
pydantic-settings>=2.6.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0
jsonschema>=4.21.1,<5.0.0
pyyaml>=6.0.2,<7.0.0
xmltodict>=0.13.0,<1.1.0
lxml>=5.3.0,<6.2.0
xmlschema>=3.4.3,<5.0.0

# -----------------------------------------------------------------------------
# File Format Support
# Note: numpy pinned <2.2 for compatibility with numba/ydata-profiling
# Note: Other file format libraries updated for stability
# -----------------------------------------------------------------------------
numpy>=1.24,<2.2
openpyxl>=3.1.5,<4.0.0
pandas>=2.2.3,<3.0.0
pyarrow>=18.1.0,<18.2.0
xlrd>=2.0.1,<3.0.0
python-docx>=1.1.2,<2.0.0

# -----------------------------------------------------------------------------
# Integrations (SOAP/OData/REST)
# Note: botocore pinned for aiobotocore compatibility
# Note: starlette updated for sse-starlette compatibility
# Note: pypdf pinned <6 for llama-index-readers-file
# Note: bcrypt updated for chromadb compatibility
# Note: protobuf updated for autogen-core/tensorflow compatibility
# Note: opensearch-py updated for stability
# Note: requests updated for compatibility
# Note: zeep and xmltodict for SOAP/XML processing
# Note: lxml with security patches
# Note: xmlschema for XML validation
# Note: jsonschema for JSON validation
# Note: PyYAML for YAML config support
# -----------------------------------------------------------------------------
requests>=2.32.3,<3.0.0
requests-oauthlib>=2.0.0,<3.0.0
oauthlib>=3.2.2,<4.0.0
zeep>=4.2.1,<5.0.0
botocore>=1.35.16,<1.35.37
starlette>=0.38.0,<0.50.0  # Compatible with fastapi 0.115.0
pypdf>=5.1.0,<6.0.0
bcrypt>=4.0.1,<5.0.0
protobuf>=5.28.0,<6.0.0
opensearch-py>=2.4.0,<3.2.1

# -----------------------------------------------------------------------------
# Authentication & Security
# -----------------------------------------------------------------------------
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
cryptography>=44.0.0,<50.0.0

# -----------------------------------------------------------------------------
# Monitoring & Logging (optional)
# -----------------------------------------------------------------------------
prometheus-client==0.21.0
sentry-sdk==2.18.0

# -----------------------------------------------------------------------------
# Background Jobs (optional)
# -----------------------------------------------------------------------------
celery==5.4.0
kombu==5.4.2

# -----------------------------------------------------------------------------
# LLM & AI Services (optional; endpoints fail closed if not configured)
# -----------------------------------------------------------------------------
openai==1.57.0
anthropic==0.39.0
ollama==0.4.4

# -----------------------------------------------------------------------------
# MCP (Model Context Protocol) (optional)
# -----------------------------------------------------------------------------
mcp[cli]>=1.2.0,<2.0.0

# -----------------------------------------------------------------------------
# Data quality (optional)
# -----------------------------------------------------------------------------
soda-core-postgres>=3.0.0,<4.0.0

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
python-dateutil==2.9.0
tenacity==9.0.0
schedule==1.2.2
cachetools==5.5.0

# -----------------------------------------------------------------------------
# Testing (kept here so there is a single requirements file)
# -----------------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# -----------------------------------------------------------------------------
# Heavy optional dependencies (commented out by default)
# -----------------------------------------------------------------------------
# Only needed if you want to generate embeddings in-process instead of via EMBEDDINGS_URL.
# sentence-transformers>=2.6.0,<4.0.0

# Spark / Distributed Compute (use in a separate Spark job environment)
# pyspark>=3.5.0,<4.0.0
