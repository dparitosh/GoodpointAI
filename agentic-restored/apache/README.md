# Apache (optional)

Apache is optional and only needed for certain reverse-proxy / static hosting deployments.

Current local dev uses:

- Frontend dev server (Vite)
- Backend API (FastAPI)

See: `../../docs/INSTALLATION.md`
